{
  "name": "@theo-startups/bgit",
  "version": "0.1.0",
  "description": "Agent-native git overlay — sessions, checkpoints, provenance",
  "type": "module",
  "bin": {
    "bgit": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "bun build src/cli.ts --outdir dist --target bun --format esm && cp -r src/schema dist/",
    "test": "bun test",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "engines": {
    "bun": ">=1.1.0"
  },
  "license": "MIT"
}
